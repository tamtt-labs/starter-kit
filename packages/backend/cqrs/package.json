{
  "name": "@tamtt-labs/cqrs",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": "./dist/index.mjs",
    "./adapters/elysia": "./dist/adapters/elysia.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "dev": "bun run build -- --watch",
    "build": "bun --bun tsdown --tsconfig tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@tamtt-labs/tsconfig": "workspace:*",
    "@tamtt-labs/tsdown": "workspace:*",
    "@types/bun": "^1.3.9",
    "elysia": "^1.4.25",
    "tsdown": "^0.20.2",
    "typescript": "^5.9.3"
  }
}
