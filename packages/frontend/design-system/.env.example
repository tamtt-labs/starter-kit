# ---------------------------------------------------------------------------
# design-system — Library-only package with no runtime configuration needed.
# This .env.example is a placeholder for consistency across the monorepo.
# ---------------------------------------------------------------------------
# No environment variables required for this package.
